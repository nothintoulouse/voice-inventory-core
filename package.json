{
  "name": "voice-inventory-core",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Deterministic speech-to-inventory core: provider-neutral ASR interfaces, a spoken count grammar, fuzzy item matching, confidence scoring, and patch-in-place XLSX export.",
  "license": "MIT",
  "scripts": {
    "demo": "tsx src/demo/run.ts",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit",
    "build": "tsc --noEmit"
  },
  "dependencies": {
    "xlsx-populate": "^1.21.0"
  },
  "devDependencies": {
    "@types/node": "^22",
    "tsx": "^4.19.2",
    "typescript": "^5.7.0",
    "vitest": "^4.1.9"
  }
}
